        <ul>

          {% for item in category %}
          	<li>
      	      	<a href="{{ path('category', {'seoTitle': item.seoTitle}) }}">{{ item.name }}</a>
    	        {% if item.categories is not empty %}
	                {% include 'frontend/category/category.html.twig' with {'category':item.categories} %}
       	        {% endif %}
             </li>
          {% endfor %}
          
          </ul>      
